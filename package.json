{
  "name": "gphone",
  "version": "1.0.0",
  "description": "An open-source TypeScript phone for FiveM",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "pnpm clearbuild && pnpm typecheck && node scripts/generate-manifests.js && node build/build-bundle.js && pnpm build:web",
    "build:web": "pnpm --filter web build",
    "clearbuild": "node -e \"const fs = require('fs'); fs.rmSync('./dist', { recursive: true, force: true });\"",
    "dev": "concurrently -c \"cyan,magenta\" -n \"fivem,vite\" \"pnpm watch\" \"pnpm watch:web\"",
    "typecheck:client": "tsc --noEmit -p client/tsconfig.json",
    "typecheck:server": "tsc --noEmit -p server/tsconfig.json",
    "typecheck:web": "pnpm --filter web check",
    "typecheck": "run-p typecheck:*",
    "watch": "pnpm clearbuild && node scripts/generate-manifests.js && cross-env IS_WATCH_MODE=1 node build/build-bundle.js",
    "watch:web": "pnpm --filter web dev"
  },
  "author": "mbiddle",
  "license": "MIT",
  "devDependencies": {
    "@citizenfx/client": "2.0.32698-1",
    "@citizenfx/server": "2.0.32561-1",
    "@types/node": "^26.1.1",
    "concurrently": "^10.0.3",
    "cross-env": "^10.1.0",
    "esbuild": "^0.28.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3"
  }
}